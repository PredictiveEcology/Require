# Needs `VersionOnRepos`, `versionSpec` and `inequality` columns

Needs `VersionOnRepos`, `versionSpec` and `inequality` columns

## Usage

``` r
availableVersionOK(pkgDT)
```

## Arguments

- pkgDT:

  A `pkgDT` object
