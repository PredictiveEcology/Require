% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{stripGitHubToRepos}
\alias{stripGitHubToRepos}
\title{Strip GitHub locators so packages resolve from \code{repos}}
\usage{
stripGitHubToRepos(pkgs, verbose = getOption("Require.verbose", 1))
}
\arguments{
\item{pkgs}{A character string vector of packages with or without GitHub path or versions}

\item{verbose}{Numeric. Controls reporting of which specs were rewritten.}
}
\value{
A character vector the same length as \code{pkgs}, with GitHub locators
removed.
}
\description{
Rewrites GitHub-style specs (\code{account/repo@branch}) to their bare package
name, preserving any version constraint. This lets \code{Require}/\code{Install}
satisfy them from \code{repos} (e.g., r-universe binaries) instead of cloning and
building from GitHub source -- avoiding the need for git authentication and
Rtools. Plain (non-GitHub) specs are returned unchanged. Used internally when
\code{getOption("Require.noRemotes")} is \code{TRUE}.
}
\examples{
Require:::stripGitHubToRepos("PredictiveEcology/SpaDES.core@development (>= 3.0.3.9003)")
# -> "SpaDES.core (>= 3.0.3.9003)"
}
\keyword{internal}
