% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Require-helpers.R
\name{internetExists}
\alias{internetExists}
\title{Internet Exists query}
\usage{
internetExists(verbose = getOption("Require.verbose"), force = FALSE)
}
\arguments{
\item{verbose}{Numeric or logical indicating how verbose should the function
be. If -1 or -2, then as little verbosity as possible. If 0 or FALSE,
then minimal outputs; if \code{1} or TRUE, more outputs; \code{2} even more. NOTE: in
\code{Require} function, when \code{verbose >= 2}, also returns details as if
\code{returnDetails = TRUE} (for backwards compatibility).}

\item{force}{If \code{TRUE}, probe even when \code{options("Require.checkInternet")}
is \code{FALSE}. Used at points where we're about to do real work (e.g.,
install dispatch) and a 2-second probe is cheap relative to the cost
of failing late. Defaults to \code{FALSE} to preserve the no-probe default
behaviour for all other call sites.}
}
\value{
Logical. \code{TRUE} if internet is available, \code{FALSE} if not.
}
\description{
Simple test for internet availability.
}
